function separateCharacters(str) {
    // Split the string into an array of characters
    return str.split('');
}

// Example usage:
let text = "hello";
let result = separateCharacters(text);
console.log("Individual characters:", result);
