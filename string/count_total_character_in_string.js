function countCharacters(str) {
    // Use the length property of the string to get the total number of characters
    return str.length;
}

// Example usage:
let text = "Hello, world!";
let totalCount = countCharacters(text);
console.log("Total number of characters:", totalCount);
